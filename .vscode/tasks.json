{
    "tasks": [
        {
            "type": "shell",
            "label": "Build with NDK",
            "command": "ndk-build",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}